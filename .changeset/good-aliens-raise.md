---
"@jamx/logger": minor
"@jamx/docs": minor
---

Add `WriterTransport`, a runtime-neutral formatted transport that sends output and the original `LogRecord` to an injected synchronous writer callback.
